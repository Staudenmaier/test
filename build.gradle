apply plugin : 'cpp'

model {
    components {
        main(NativeExecutableSpec)   {
          binaries.all {
    				cppCompiler.args "-std=c++11"
            linker.args "-lpthread"
          }
        }
    }
}
